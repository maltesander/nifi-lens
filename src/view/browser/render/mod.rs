//! Browser tab render modules.
//!
//! Per-kind detail pane renderers (`pg`, `processor`, `connection`,
//! `controller_service`) land in Task 18. Until then, this file is
//! empty — the Phase 0 placeholder is drawn from `browser::render` in
//! `mod.rs`.
